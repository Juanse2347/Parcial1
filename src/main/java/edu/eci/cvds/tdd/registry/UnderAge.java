package edu.eci.cvds.tdd.registry;

class PersonaEdadMinima {

    public boolean EdadMinima (int age) throws Exception {
        /*
        if (18 <= age && age <= 160) {
            return false;
        } else if(0 <= age && age <= 17){
            return true;
        }else {
            throw new Exception();
        }

         */
        return false;
    }
    }

